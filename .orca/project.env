# backend-init 프로젝트 고유값 정본 (2026-09-12, ~/.backend-init/coord/backlog.md 의 값을 옮김)
# launch-worker.sh 가 워커 TASK.md 의 {{자리표시자}} 를 채우고, verify-pr.sh 가 GUARD_CMD 를 읽는다. 비밀은 넣지 않는다.
PROD_NOTE='운영 프로세스·실 DB 없음. 포트 8080/8081/8082/5432/6380 은 워커가 기동 금지'
TEST_DSN='없음 — livePostgresTest 금지, Docker 불필요'
CHECK_CMDS='./gradlew build → ./gradlew -p build-logic test → git diff --check'
GUARD_CMD='./gradlew :core:application:test --tests "cc.midolog.ApplicationPackageStructureTest"'
VERIFY_HOST=''
PM_RUN=''
