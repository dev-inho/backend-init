server:
  port: 8080

spring:
  application:
    name: backend-gateway
  profiles:
    active: local

gateway:
  routes:
    application-url: http://localhost:8081
    batch-url: http://localhost:8082

jwt:
  secret: ${JWT_SECRET}
